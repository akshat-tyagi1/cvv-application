function Summary({ summary }) {
  return (
    <section className="resume-section">
      <h2>SUMMARY</h2>

      <div>
        <p>{summary}</p>
      </div>
    </section>
  );
}

export default Summary;
